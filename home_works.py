import random  #Импорт всего файла/модуля
a = random.randint(0, 10)
from random import randint # Импорт конкретных объектов из модуля
x = randint(0, 10)
import datetime as dt # Импорт всего модуля с псевдонимом
now = dt.datetime.now()
from random import * # Импорт всех объектов из модуля